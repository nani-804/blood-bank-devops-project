# Kubernetes Deployment

This folder contains Kubernetes YAML files for Blood Bank application deployment.

## Files
- deployment.yaml
- service.yaml

## Commands

kubectl apply -f deployment.yaml

kubectl apply -f service.yaml
